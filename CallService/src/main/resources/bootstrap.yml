spring:
  application:
    name: CallService
  profiles:
    active: dev
  cloud:
    nacos:
      config:

        file-extension: yaml
        #group: testnacos
        #namespace: A61
      server-addr: 127.0.0.1:10086
      discovery:
        cluster-name: FJ #杭州